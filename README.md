# my nvim setup on Mac

I love neovim

# Prerequisites

Install neovim, ripgrep(for telescope), node

```bash
brew install nvim
brew install ripgrep
brew install node
```

Download

```bash
cd ~/.config
git clone [url]
mv nvim-setup nvim
```
